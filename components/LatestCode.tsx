import { useEffect, useState } from "react";

export default function LatestCode() {
  const [repos, setRepos] = useState();

  useEffect(() => {}, []);

  return (
    <section>
      <div></div>
    </section>
  );
}
